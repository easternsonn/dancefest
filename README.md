# 🎭 Арт-портал — Платформа для развития хореографического искусства

Современный адаптивный веб-сайт для танцевальной платформы с функционалом подачи заявок и загрузки музыкальных файлов.

## 📋 Содержание

- [Особенности](#особенности)
- [Структура проекта](#структура-проекта)
- [Технологии](#технологии)
- [Установка и запуск](#установка-и-запуск)
- [Настройка отправки email](#настройка-отправки-email)
- [Деплой](#деплой)
- [Браузерная поддержка](#браузерная-поддержка)

## ✨ Особенности

### Дизайн и UI
- ✅ Современный минималистичный дизайн
- ✅ Светло-фиолетовая цветовая гамма
- ✅ Плавные анимации и hover-эффекты
- ✅ Чистая типографика с использованием Inter
- ✅ Адаптивная верстка (mobile / tablet / desktop)

### Функциональность
- ✅ Фиксированная навигация с плавной прокруткой
- ✅ Мобильное бургер-меню
- ✅ Форма заявки с валидацией полей
- ✅ Загрузка MP3 файлов (до 50MB)
- ✅ Отправка данных и файлов на email
- ✅ Уведомления об успешной/неуспешной отправке

### Валидация формы
- Имя: минимум 2 символа
- Email: корректный формат email
- Телефон: автоформатирование, 10-11 цифр
- Проект: обязательный выбор
- MP3 файл: обязательный, максимум 50MB

## 📁 Структура проекта

```
dancefest/
│
├── index.html              # Главная страница (HTML5)
│
├── css/
│   └── style.css          # Стили (CSS3, Flexbox, Grid)
│
├── js/
│   └── main.js            # JavaScript (валидация, загрузка файлов)
│
├── submit.php             # PHP backend для отправки формы
│
├── server.js              # Node.js backend (альтернатива PHP)
│
├── package.json           # Зависимости Node.js
│
├── env.example            # Пример конфигурации email
│
├── logo.png               # Логотип компании
│
└── README.md              # Документация
```

## 🛠 Технологии

### Frontend
- **HTML5** — семантическая разметка
- **CSS3** — современные стили (CSS Variables, Flexbox, Grid)
- **JavaScript (ES6+)** — валидация, AJAX, DOM manipulation
- **Google Fonts** — Inter (400-800)

### Backend (два варианта)

#### Вариант 1: PHP
- PHP 7.4+
- Встроенная функция `mail()` или PHPMailer

#### Вариант 2: Node.js
- Express.js — веб-сервер
- Multer — загрузка файлов
- Nodemailer — отправка email
- CORS — cross-origin запросы
- dotenv — управление переменными окружения

## 🚀 Установка и запуск

### Вариант 1: PHP Backend

#### Требования
- PHP 7.4 или выше
- Веб-сервер (Apache/Nginx)
- Настроенная функция `mail()` или SMTP

#### Установка

1. **Загрузите файлы на сервер**
   ```bash
   # Через FTP или SSH загрузите все файлы в корневую директорию сайта
   ```

2. **Настройте submit.php**
   ```php
   // Откройте submit.php и измените:
   define('RECIPIENT_EMAIL', 'your-email@example.com');
   ```

3. **Настройте права доступа**
   ```bash
   chmod 755 submit.php
   ```

4. **Готово!** Откройте сайт в браузере

#### Настройка SMTP для PHP (если mail() не работает)

Используйте PHPMailer:
```bash
composer require phpmailer/phpmailer
```

Пример кода в `submit.php`:
```php
use PHPMailer\PHPMailer\PHPMailer;

$mail = new PHPMailer(true);
$mail->isSMTP();
$mail->Host = 'smtp.gmail.com';
$mail->SMTPAuth = true;
$mail->Username = 'your-email@gmail.com';
$mail->Password = 'your-app-password';
$mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
$mail->Port = 587;
```

---

### Вариант 2: Node.js Backend (рекомендуется)

#### Требования
- Node.js 14+ и npm

#### Установка

1. **Клонируйте или скачайте проект**
   ```bash
   cd /path/to/dancefest
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   ```bash
   # Скопируйте файл env.example
   cp env.example .env
   
   # Откройте .env и заполните данные:
   nano .env
   ```

   Пример `.env`:
   ```env
   PORT=3000
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password
   RECIPIENT_EMAIL=recipient@example.com
   ```

4. **Обновите путь в main.js**
   
   Откройте `js/main.js` и измените URL отправки:
   ```javascript
   // Строка 312: измените 'submit.php' на '/submit'
   const response = await fetch('/submit', {
       method: 'POST',
       body: formData
   });
   ```

5. **Запустите сервер**
   ```bash
   npm start
   ```
   
   Или для разработки с автоперезагрузкой:
   ```bash
   npm run dev
   ```

6. **Откройте браузер**
   ```
   http://localhost:3000
   ```

## 📧 Настройка отправки email

### Gmail

1. **Включите двухфакторную аутентификацию**
   - Перейдите: https://myaccount.google.com/security
   - Включите 2FA

2. **Создайте пароль приложения**
   - Перейдите: https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Скопируйте сгенерированный пароль

3. **Используйте в конфигурации**
   ```env
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=generated-app-password
   ```

### Yandex Mail

```env
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=your-email@yandex.ru
SMTP_PASS=your-password
```

### Mail.ru

```env
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
SMTP_USER=your-email@mail.ru
SMTP_PASS=your-password
```

### Outlook/Hotmail

```env
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=your-email@outlook.com
SMTP_PASS=your-password
```

## 🌐 Деплой

### Деплой на хостинг с PHP

1. Загрузите все файлы через FTP
2. Настройте `submit.php` с вашим email
3. Убедитесь, что PHP может отправлять письма
4. Готово!

### Деплой Node.js приложения

#### Heroku

```bash
# Установите Heroku CLI
heroku login
heroku create your-app-name

# Добавьте переменные окружения
heroku config:set SMTP_HOST=smtp.gmail.com
heroku config:set SMTP_PORT=587
heroku config:set SMTP_USER=your-email@gmail.com
heroku config:set SMTP_PASS=your-password
heroku config:set RECIPIENT_EMAIL=recipient@example.com

# Деплой
git push heroku main
```

#### DigitalOcean / VPS

```bash
# Подключитесь к серверу
ssh user@your-server-ip

# Клонируйте репозиторий
git clone your-repo-url
cd dancefest

# Установите зависимости
npm install

# Настройте .env файл
nano .env

# Установите PM2 для управления процессом
npm install -g pm2
pm2 start server.js --name "art-portal"
pm2 startup
pm2 save

# Настройте Nginx как reverse proxy
sudo nano /etc/nginx/sites-available/artportal

# Пример конфигурации:
server {
    listen 80;
    server_name yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Активируйте конфигурацию
sudo ln -s /etc/nginx/sites-available/artportal /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### Vercel (только frontend + внешний API)

```bash
npm install -g vercel
vercel
```

**Примечание:** Для Vercel нужно использовать serverless functions или внешний API для обработки форм.

## 🌍 Браузерная поддержка

| Браузер | Версия |
|---------|--------|
| Chrome | ✅ Последние 2 версии |
| Firefox | ✅ Последние 2 версии |
| Safari | ✅ 12+ |
| Edge | ✅ Последние 2 версии |
| Opera | ✅ Последние 2 версии |

## 📱 Адаптивность

- **Desktop:** 1200px+
- **Tablet:** 768px - 1199px
- **Mobile:** < 768px
- **Small Mobile:** < 480px

## 🔒 Безопасность

### Реализованные меры

- ✅ Валидация данных на клиенте и сервере
- ✅ Защита от SQL инъекций (нет БД)
- ✅ Санитизация пользовательского ввода
- ✅ Ограничение размера файла (50MB)
- ✅ Проверка типа файла (только MP3)
- ✅ CORS настройки
- ✅ Переменные окружения для чувствительных данных

### Рекомендации

- Используйте HTTPS в продакшене
- Добавьте rate limiting для защиты от спама
- Используйте CAPTCHA (Google reCAPTCHA)
- Регулярно обновляйте зависимости

## 🐛 Решение проблем

### Форма не отправляется

1. **Проверьте консоль браузера** (F12)
2. **Убедитесь, что backend работает**
   - PHP: проверьте логи сервера
   - Node.js: проверьте `node server.js`
3. **Проверьте SMTP настройки**
4. **Проверьте права доступа к файлам**

### Email не приходят

1. **Проверьте папку "Спам"**
2. **Проверьте SMTP credentials**
3. **Для Gmail:** убедитесь, что используете App Password
4. **Проверьте логи сервера**

### Файл не загружается

1. **Проверьте размер файла** (макс. 50MB)
2. **Убедитесь, что это MP3 файл**
3. **Проверьте права на запись** (для PHP/Node.js)
4. **Увеличьте лимиты в php.ini** или server.js

## 📞 Поддержка

По вопросам и предложениям:
- Email: support@artportal.ru
- Website: https://artportal.ru
- ВКонтакте: @artportal

## 📄 Лицензия

© 2026 Арт-портал. Все права защищены.

---

**Создано с ❤️ для танцевального сообщества**
