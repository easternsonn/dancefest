# 🚀 Быстрый старт — Арт-портал

## Самый быстрый способ запустить сайт

### ⚡️ Для PHP хостинга (5 минут)

1. **Скачайте проект и распакуйте**

2. **Настройте email в `submit.php`**
   ```php
   // Строка 22: измените на ваш email
   define('RECIPIENT_EMAIL', 'your-email@example.com');
   ```

3. **Загрузите файлы на хостинг через FTP**
   - Загрузите все файлы в корневую директорию (например: `public_html/`)

4. **Готово!** Откройте сайт в браузере

---

### ⚡️ Для Node.js (10 минут)

1. **Установите Node.js** (если еще не установлен)
   - Скачайте с https://nodejs.org/

2. **Откройте терминал в папке проекта**
   ```bash
   cd /path/to/dancefest
   ```

3. **Установите зависимости**
   ```bash
   npm install
   ```

4. **Создайте файл `.env`**
   ```bash
   cp env.example .env
   ```
   
   Откройте `.env` и заполните:
   ```env
   PORT=3000
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASS=your-app-password
   RECIPIENT_EMAIL=where-to-receive@example.com
   ```

5. **Запустите сервер**
   ```bash
   npm start
   ```

6. **Откройте браузер**
   ```
   http://localhost:3000
   ```

---

## 📧 Настройка Gmail (3 минуты)

### Для получения App Password:

1. Откройте: https://myaccount.google.com/security
2. Включите **"Двухэтапная аутентификация"**
3. Откройте: https://myaccount.google.com/apppasswords
4. Выберите:
   - Приложение: **Почта**
   - Устройство: **Другое** (введите: "Арт-портал")
5. Нажмите **"Создать"**
6. Скопируйте **16-значный пароль**
7. Используйте этот пароль в настройках (не ваш обычный пароль!)

---

## ✅ Проверка работы

### Тест формы:

1. Откройте сайт
2. Прокрутите до формы "Подать заявку"
3. Заполните все поля:
   - Имя: `Иван Иванов`
   - Email: `test@example.com`
   - Телефон: `+7 (999) 123-45-67`
   - Проект: выберите любой
   - Сообщение: `Тестовая заявка`
   - MP3 файл: загрузите любой MP3 файл
4. Нажмите **"Отправить заявку"**
5. Должно появиться сообщение: **"Спасибо! Ваша заявка успешно отправлена"**
6. Проверьте почту (указанную в RECIPIENT_EMAIL)

---

## 🐛 Проблемы?

### Форма не отправляется?
- Откройте консоль браузера (F12)
- Посмотрите вкладку "Console" и "Network"
- Найдите ошибки красного цвета

### Email не приходит?
1. Проверьте папку **"Спам"**
2. Проверьте настройки SMTP в `.env` или `submit.php`
3. Убедитесь, что используете **App Password**, а не обычный пароль Gmail

### Ошибка "SMTP connection failed"?
- Проверьте, что включена двухэтапная аутентификация в Gmail
- Проверьте, что правильно скопировали App Password (без пробелов)
- Попробуйте отключить антивирус/файрвол

---

## 📞 Нужна помощь?

Прочитайте полную документацию: **README.md**

Или напишите на: support@artportal.ru

---

**Удачного запуска! 🎭**
