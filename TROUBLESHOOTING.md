# 🔧 Решение проблем — Арт-портал

## Частые проблемы и их решения

### 🚫 Форма не отправляется

#### Симптомы:
- При нажатии "Отправить заявку" ничего не происходит
- Кнопка остаётся в состоянии "Отправка..."
- Появляется сообщение об ошибке

#### Решение:

**1. Откройте консоль браузера**
```
Windows/Linux: F12 или Ctrl+Shift+I
Mac: Cmd+Option+I
```

**2. Перейдите на вкладку "Console"**
- Посмотрите на красные ошибки
- Часто там указана причина проблемы

**3. Проверьте вкладку "Network"**
- Обновите страницу (F5)
- Заполните и отправьте форму снова
- Найдите запрос к `submit.php` или `/submit`
- Кликните на него и посмотрите:
  - Status Code (должен быть 200)
  - Response (ответ сервера)

**4. Частые ошибки в консоли:**

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `404 Not Found` | Файл `submit.php` не найден | Проверьте, что файл загружен на сервер |
| `500 Internal Server Error` | Ошибка в PHP коде | Проверьте логи сервера |
| `CORS error` | Проблема с доменами | Настройте CORS в backend |
| `Failed to fetch` | Сервер не отвечает | Убедитесь, что сервер запущен |

---

### 📧 Email не приходит

#### Проверьте папку "Спам"
Первым делом проверьте папку "Спам" или "Нежелательная почта"

#### Для PHP версии:

**1. Проверьте `submit.php`**
```php
// Строка 22: правильный ли email?
define('RECIPIENT_EMAIL', 'your-email@example.com');
```

**2. Проверьте, работает ли функция mail()**
Создайте файл `test-mail.php`:
```php
<?php
$to = 'your-email@example.com';
$subject = 'Test Email';
$message = 'This is a test email';
$headers = 'From: noreply@yourdomain.com';

if (mail($to, $subject, $message, $headers)) {
    echo 'Email sent successfully';
} else {
    echo 'Email sending failed';
}
?>
```

Откройте в браузере: `https://yourdomain.com/test-mail.php`

**3. Если mail() не работает:**
- Свяжитесь с хостинг-провайдером
- Или используйте SMTP через PHPMailer

#### Для Node.js версии:

**1. Проверьте `.env` файл**
```env
SMTP_HOST=smtp.gmail.com     # Правильный ли host?
SMTP_PORT=587                # Правильный ли port?
SMTP_USER=your@gmail.com     # Правильный ли email?
SMTP_PASS=app-password-here  # Правильный ли пароль?
RECIPIENT_EMAIL=where@email.com  # Куда отправлять?
```

**2. Для Gmail — используйте App Password**
- НЕ используйте обычный пароль от аккаунта
- Включите двухфакторную аутентификацию
- Создайте App Password: https://myaccount.google.com/apppasswords
- Используйте этот 16-значный пароль

**3. Проверьте логи сервера**
```bash
# В терминале, где запущен сервер
# Вы должны увидеть ошибки, если что-то не так
```

**4. Тест SMTP подключения**
Создайте файл `test-smtp.js`:
```javascript
const nodemailer = require('nodemailer');
require('dotenv').config();

const transporter = nodemailer.createTransport({
    host: process.env.SMTP_HOST,
    port: process.env.SMTP_PORT,
    auth: {
        user: process.env.SMTP_USER,
        pass: process.env.SMTP_PASS
    }
});

transporter.verify((error, success) => {
    if (error) {
        console.error('❌ SMTP Error:', error);
    } else {
        console.log('✅ SMTP connection successful!');
    }
});
```

Запустите: `node test-smtp.js`

---

### 🔴 Ошибка "SMTP connection failed"

#### Причины и решения:

**1. Неправильные credentials**
```env
# Проверьте:
SMTP_USER=your-correct-email@gmail.com
SMTP_PASS=your-16-digit-app-password  # БЕЗ ПРОБЕЛОВ!
```

**2. Блокировка файрволом/антивирусом**
- Временно отключите антивирус
- Проверьте настройки файрвола
- Убедитесь, что порты 587 или 465 не заблокированы

**3. Неправильный порт**
```env
# Для большинства SMTP:
SMTP_PORT=587  # TLS
# Или:
SMTP_PORT=465  # SSL
```

**4. Gmail блокирует доступ**
- Перейдите: https://myaccount.google.com/security
- Убедитесь, что "Двухэтапная аутентификация" включена
- Создайте новый App Password
- Используйте его в `.env`

**5. Используете неправильный SMTP сервер**
```env
# Gmail:
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Yandex:
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465

# Mail.ru:
SMTP_HOST=smtp.mail.ru
SMTP_PORT=465
```

---

### 📁 Ошибка загрузки файла

#### "Разрешены только MP3 файлы"

**Причина:** Файл имеет неправильный формат

**Решение:**
- Убедитесь, что файл действительно MP3
- Проверьте расширение файла (должно быть `.mp3`)
- Конвертируйте файл в MP3, если это не так

#### "Размер файла превышает 50MB"

**Причина:** Файл слишком большой

**Решение:**
- Сожмите MP3 файл (уменьшите битрейт)
- Или увеличьте лимит в коде:

Для PHP (`submit.php`):
```php
// Строка 26
define('MAX_FILE_SIZE', 100 * 1024 * 1024); // 100MB
```

Также в `php.ini`:
```ini
upload_max_filesize = 100M
post_max_size = 100M
```

Для Node.js (`server.js`):
```javascript
// Строка 57
limits: {
    fileSize: 100 * 1024 * 1024 // 100MB
}
```

#### "Ошибка при загрузке файла"

**Проверьте права доступа к папке**

Для PHP:
```bash
# Если используете папку uploads/
chmod 755 uploads/
chown www-data:www-data uploads/
```

Для Node.js:
```bash
# Папка создаётся автоматически, но проверьте:
ls -la uploads/
```

---

### 🌐 Сайт не открывается

#### "This site can't be reached"

**Для локальной разработки:**
1. Убедитесь, что сервер запущен:
   ```bash
   npm start
   # Должно быть: Server running on http://localhost:3000
   ```

2. Откройте правильный адрес:
   ```
   http://localhost:3000
   ```

**Для продакшен сервера:**
1. Проверьте, что домен указывает на ваш сервер
2. Проверьте DNS записи (может занять до 48 часов)
3. Проверьте, что веб-сервер запущен:
   ```bash
   sudo systemctl status nginx
   sudo systemctl status apache2
   ```

#### Белый экран / ничего не отображается

**1. Проверьте консоль браузера (F12)**
- Смотрите на ошибки загрузки CSS/JS

**2. Проверьте пути к файлам**
```html
<!-- В index.html - правильные ли пути? -->
<link rel="stylesheet" href="css/style.css">
<script src="js/main.js"></script>
```

**3. Проверьте структуру папок**
```
public_html/
├── index.html
├── css/
│   └── style.css
└── js/
    └── main.js
```

---

### 📱 Проблемы на мобильных устройствах

#### Меню не открывается

**1. Проверьте JavaScript загрузился**
- Откройте мобильную консоль (через Chrome DevTools)
- Проверьте наличие ошибок

**2. Проверьте, что файл main.js подключен**
```html
<!-- В конце body, перед </body> -->
<script src="js/main.js"></script>
```

#### Элементы накладываются друг на друга

**1. Очистите кэш браузера**
- Мобильный Safari: Настройки → Safari → Очистить историю
- Chrome Android: Настройки → Приватность → Очистить данные

**2. Проверьте адаптивность**
```css
/* В style.css должны быть media queries */
@media (max-width: 768px) {
    /* мобильные стили */
}
```

---

### 🔧 Общие советы по отладке

#### 1. Включите режим разработчика
```
Chrome: F12
Firefox: F12
Safari: Cmd+Option+I (нужно включить в настройках)
```

#### 2. Используйте тестовую страницу
Откройте `test-form.html` — это упрощённая версия для тестирования

#### 3. Проверьте логи сервера

**PHP:**
```bash
# Обычно в:
tail -f /var/log/apache2/error.log
tail -f /var/log/nginx/error.log
```

**Node.js:**
```bash
# В терминале, где запущен сервер
# Все ошибки будут выводиться в консоль
```

#### 4. Тестируйте поэтапно

1. ✅ Сайт открывается?
2. ✅ CSS загружается? (проверьте, что дизайн применён)
3. ✅ JavaScript работает? (меню открывается?)
4. ✅ Форма отправляется? (появляется сообщение?)
5. ✅ Email приходит?

#### 5. Используйте curl для тестирования API

```bash
# Тест отправки формы
curl -X POST http://localhost:3000/submit \
  -F "name=Test User" \
  -F "email=test@example.com" \
  -F "phone=+7 999 123-45-67" \
  -F "project=korni" \
  -F "message=Test message" \
  -F "mp3file=@/path/to/test.mp3"
```

---

## 🆘 Всё ещё не работает?

### Чек-лист перед обращением за помощью:

- [ ] Проверил консоль браузера (F12)
- [ ] Проверил логи сервера
- [ ] Проверил все конфигурационные файлы
- [ ] Попробовал тестовую страницу (test-form.html)
- [ ] Проверил, что все файлы загружены на сервер
- [ ] Проверил права доступа к файлам
- [ ] Очистил кэш браузера
- [ ] Попробовал в режиме инкогнито
- [ ] Попробовал в другом браузере

### Подготовьте информацию:

1. **Ваше окружение:**
   - Операционная система
   - Браузер и версия
   - Версия PHP или Node.js
   - Хостинг-провайдер

2. **Ошибка:**
   - Скриншот ошибки
   - Текст из консоли браузера
   - Логи сервера (если есть)

3. **Что вы уже пробовали:**
   - Список действий, которые вы предприняли

### Контакты для поддержки:

- Email: support@artportal.ru
- Документация: README.md
- GitHub Issues: (если проект на GitHub)

---

**Последнее обновление:** 2026-01-29
