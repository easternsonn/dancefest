# ✅ Чеклист для деплоя — Арт-портал

## Перед деплоем

### 📝 Контент
- [ ] Заменить `logo.png` на реальный логотип компании
- [ ] Проверить все тексты на опечатки
- [ ] Добавить реальные изображения (если нужны)
- [ ] Проверить контактную информацию
- [ ] Обновить ссылки на соцсети (VK, Telegram, Instagram)

### ⚙️ Конфигурация

#### Для PHP версии:
- [ ] Открыть `submit.php`
- [ ] Изменить `RECIPIENT_EMAIL` на реальный email
- [ ] Проверить настройки SMTP (если используете)
- [ ] Протестировать отправку email на локальном сервере

#### Для Node.js версии:
- [ ] Создать `.env` файл из `env.example`
- [ ] Заполнить все переменные окружения:
  - [ ] `SMTP_HOST` — SMTP сервер
  - [ ] `SMTP_PORT` — порт (587 или 465)
  - [ ] `SMTP_USER` — email для отправки
  - [ ] `SMTP_PASS` — пароль (App Password для Gmail)
  - [ ] `RECIPIENT_EMAIL` — email для получения заявок
- [ ] Протестировать локально: `npm start`
- [ ] Проверить отправку тестовой заявки

### 🔐 Безопасность
- [ ] Добавить `.env` в `.gitignore` (уже добавлен)
- [ ] Не коммитить пароли и секреты в Git
- [ ] Использовать HTTPS в продакшене
- [ ] Настроить CORS правильно (только нужные домены)
- [ ] Проверить права доступа к файлам (PHP: 755 для папок, 644 для файлов)

### 🎨 UI/UX
- [ ] Протестировать на Desktop (Chrome, Firefox, Safari, Edge)
- [ ] Протестировать на планшете (iPad, Android)
- [ ] Протестировать на смартфоне (iPhone, Android)
- [ ] Проверить все ссылки работают
- [ ] Проверить плавную прокрутку
- [ ] Проверить мобильное меню открывается/закрывается
- [ ] Проверить hover-эффекты на карточках
- [ ] Проверить валидацию формы (все поля)
- [ ] Проверить загрузку MP3 файла

---

## Деплой на хостинг

### 📤 PHP хостинг (cPanel, Plesk, etc.)

#### Шаг 1: Загрузка файлов
- [ ] Подключиться к FTP (FileZilla, Cyberduck, или файловый менеджер cPanel)
- [ ] Загрузить все файлы в `public_html/` или корневую папку сайта
- [ ] Убедиться, что структура папок сохранена:
  ```
  public_html/
  ├── index.html
  ├── css/
  ├── js/
  ├── submit.php
  └── logo.png
  ```

#### Шаг 2: Настройка прав доступа
- [ ] Установить права для папок: `755`
- [ ] Установить права для файлов: `644`
- [ ] `submit.php` должен быть исполняемым (755 или 644, зависит от хостинга)

#### Шаг 3: Проверка PHP
- [ ] Убедиться, что PHP 7.4+ установлен
- [ ] Проверить, что функция `mail()` работает
- [ ] Или настроить SMTP через `PHPMailer`

#### Шаг 4: Тестирование
- [ ] Открыть сайт в браузере: `https://yourdomain.com`
- [ ] Заполнить и отправить тестовую заявку
- [ ] Проверить получение email

---

### 🚀 Node.js хостинг

#### Вариант A: VPS (DigitalOcean, AWS, etc.)

**Шаг 1: Подключение к серверу**
```bash
ssh user@your-server-ip
```

**Шаг 2: Установка Node.js**
```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# Проверка
node --version
npm --version
```

**Шаг 3: Клонирование проекта**
```bash
cd /var/www
git clone your-repo-url dancefest
cd dancefest
npm install
```

**Шаг 4: Настройка окружения**
```bash
nano .env
# Заполнить все переменные
```

**Шаг 5: Установка PM2**
```bash
sudo npm install -g pm2
pm2 start server.js --name "art-portal"
pm2 startup
pm2 save
```

**Шаг 6: Настройка Nginx**
```bash
sudo nano /etc/nginx/sites-available/artportal
```

Добавить:
```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/artportal /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

**Шаг 7: SSL (Certbot)**
```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d yourdomain.com -d www.yourdomain.com
```

**Чеклист:**
- [ ] Node.js установлен
- [ ] Проект загружен
- [ ] `.env` настроен
- [ ] PM2 запущен
- [ ] Nginx настроен
- [ ] SSL сертификат установлен
- [ ] Firewall настроен (порты 80, 443, 22)
- [ ] Сайт открывается по домену

---

#### Вариант B: Heroku

**Шаг 1: Установка Heroku CLI**
```bash
# macOS
brew tap heroku/brew && brew install heroku

# Windows/Linux
# Скачать с https://devcenter.heroku.com/articles/heroku-cli
```

**Шаг 2: Логин и создание приложения**
```bash
heroku login
heroku create art-portal-dance
```

**Шаг 3: Добавление переменных окружения**
```bash
heroku config:set SMTP_HOST=smtp.gmail.com
heroku config:set SMTP_PORT=587
heroku config:set SMTP_USER=your-email@gmail.com
heroku config:set SMTP_PASS=your-app-password
heroku config:set RECIPIENT_EMAIL=recipient@example.com
```

**Шаг 4: Деплой**
```bash
git init
git add .
git commit -m "Initial deploy"
git push heroku main
```

**Шаг 5: Открыть приложение**
```bash
heroku open
```

**Чеклист:**
- [ ] Heroku CLI установлен
- [ ] Приложение создано
- [ ] Переменные окружения добавлены
- [ ] Код задеплоен
- [ ] Приложение работает
- [ ] Форма отправляет email

---

#### Вариант C: Vercel (только Frontend)

> **Примечание:** Vercel отлично подходит для статических сайтов, но для обработки формы потребуется создать Serverless Function или использовать внешний API.

```bash
npm install -g vercel
vercel
```

**Для формы на Vercel:**
- Использовать Vercel Serverless Functions
- Или использовать сервис FormSpree / Getform
- Или подключить внешний API

---

## После деплоя

### 🧪 Финальное тестирование

- [ ] Открыть сайт в режиме инкогнито
- [ ] Проверить скорость загрузки (Google PageSpeed Insights)
- [ ] Протестировать форму с реальными данными
- [ ] Проверить получение email
- [ ] Проверить, что MP3 файл приходит корректно
- [ ] Протестировать на мобильных устройствах
- [ ] Проверить все ссылки в навигации
- [ ] Проверить адаптивность на разных экранах

### 📊 Аналитика (опционально)

- [ ] Добавить Google Analytics
- [ ] Добавить Yandex Metrika
- [ ] Настроить цели (отправка формы)
- [ ] Добавить Facebook Pixel (если нужно)

### 🔒 Дополнительная безопасность

- [ ] Добавить Google reCAPTCHA v3
- [ ] Настроить Rate Limiting
- [ ] Добавить CSP (Content Security Policy)
- [ ] Регулярно обновлять зависимости: `npm update`

### 📈 SEO

- [ ] Добавить `robots.txt`
- [ ] Добавить `sitemap.xml`
- [ ] Проверить Open Graph теги
- [ ] Отправить сайт в Google Search Console
- [ ] Отправить сайт в Яндекс Вебмастер

---

## 📝 robots.txt (создать в корне)

```txt
User-agent: *
Allow: /

Sitemap: https://yourdomain.com/sitemap.xml
```

## 🗺️ sitemap.xml (создать в корне)

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://yourdomain.com/</loc>
    <lastmod>2026-01-29</lastmod>
    <priority>1.0</priority>
  </url>
</urlset>
```

---

## ✅ Готово к запуску!

Если все пункты отмечены, сайт готов к работе! 🎉

---

**Последняя проверка:** 2026-01-29
